# Optional chat API endpoint for the assistant bot
# Set this to a server endpoint that accepts { message: string } and returns { reply: string }
VITE_CHAT_API_URL=https://your-chat-api.example.com/chat
